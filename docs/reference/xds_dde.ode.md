# Set `xds` to `dde`

Change `xds` from `ode` to `dde`

## Usage

``` r
# S3 method for ode
xds_dde(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
