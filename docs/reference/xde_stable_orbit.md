# Compute the stable orbit for a system of differential equations

This method dispatches on the type of `pars$xde`.

## Usage

``` r
xde_stable_orbit(pars, Ymax = 10)
```

## Arguments

- pars:

  an **`xds`** object

- Ymax:

  the number of years to burn-in

## Value

an **`xds`** object
