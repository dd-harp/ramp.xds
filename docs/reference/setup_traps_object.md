# Setup the Junction for Exogenous traps

This sets up the `none` option for exogenous traps: no traps.

## Usage

``` r
setup_traps_object(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** object
