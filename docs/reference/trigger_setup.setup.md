# Trigger `setup`

For various `S3` functions, change leave the class of the dispatching
object as `setup`

## Usage

``` r
# S3 method for class 'setup'
trigger_setup(obj)
```

## Arguments

- obj:

  part of an **`xds`** object

## Value

the `obj`
