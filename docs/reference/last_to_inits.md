# Set the initial values to the last values of the last simulation

Set the initial values to the last values of the last simulation

## Usage

``` r
last_to_inits(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** model object
