# Compute beta

This function computes the mixing matrix, beta

## Usage

``` r
make_beta(t, y, pars)
```

## Arguments

- t:

  current simulation time

- y:

  state vector

- pars:

  an `xds` object

## Value

an `xds` object
