# Get mean forcing

Get `Lambda`, the mean emergence rate(s) \\\Lambda\\, for a model forced
by adult mosquito emergence

## Usage

``` r
# S3 method for class 'Lambda'
get_mean_forcing(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object
