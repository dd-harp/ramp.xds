# Set `xds` for `dde`

Do not change `xds`

## Usage

``` r
# S3 method for dde
xds_dde(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
