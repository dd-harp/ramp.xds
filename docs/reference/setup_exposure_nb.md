# Set up the negative binomial model of exposure

Set up the negative binomial model of exposure

## Usage

``` r
setup_exposure_nb(pars, sz)
```

## Arguments

- pars:

  an `xds` object

- sz:

  the negative binomial size parameter (see
  [pnbinom](https://rdrr.io/r/stats/NegBinomial.html))

## Value

an `xds` object

## See also

[F_foi.nb](https://dd-harp.github.io/ramp.xds/reference/F_foi.nb.md) and
[F_ar.nb](https://dd-harp.github.io/ramp.xds/reference/F_ar.nb.md)
