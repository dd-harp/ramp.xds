# Get seasonal pattern

Get the parameters that set the seasonal pattern for an **MY** trivial
model

## Usage

``` r
# S3 method for class 'MY'
get_season(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

seasonal pattern parameters
