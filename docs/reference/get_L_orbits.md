# Get orbits (**L**)

Pull the saved, parsed orbits for the **L** component from an **`xds`**
object

## Usage

``` r
get_L_orbits(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

the orbits for the **L** component
