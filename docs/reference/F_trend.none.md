# Plot the temporal trend

The `F_trend` case for models with no forcing.

## Usage

``` r
# S3 method for class 'none'
F_trend(tm, xds_obj)
```

## Arguments

- tm:

  the time points

- xds_obj:

  an **`xds`** model object

## Value

a null result
