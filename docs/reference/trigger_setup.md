# Trigger `setup`

For various `S3` functions, change the class of the dispatching object
from `static` to `setup`

## Usage

``` r
trigger_setup(obj)
```

## Arguments

- obj:

  an object

## Value

the `obj`
