# Compute and store \\\cal U\\

Compute and store the egg distribution matrices for all the vector
species

## Usage

``` r
make_calU(pars)
```

## Arguments

- pars:

  an `xds` object

## Value

an `xds` object
