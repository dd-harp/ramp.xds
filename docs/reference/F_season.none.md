# Plot the seasonal pattern

The `F_season` case for models with no forcing.

## Usage

``` r
# S3 method for class 'none'
F_season(tm, xds_obj)
```

## Arguments

- tm:

  the time points

- xds_obj:

  an **`xds`** model object

## Value

a null result
