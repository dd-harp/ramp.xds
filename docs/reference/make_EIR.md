# Compute EIR

This function computes the EIR for each stratum of each host species

## Usage

``` r
make_EIR(t, y, pars)
```

## Arguments

- t:

  current simulation time

- y:

  state vector

- pars:

  an `xds` object

## Value

an `xds` object
