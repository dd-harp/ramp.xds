# Compute kappa

This function computes kappa for each vector species in each patch

## Usage

``` r
make_kappa(t, y, pars)
```

## Arguments

- t:

  current simulation time

- y:

  state vector

- pars:

  an `xds` object

## Value

an `xds` object
