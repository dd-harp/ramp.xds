# Get mean forcing

Get the parameter(s) that set mean forcing

## Usage

``` r
get_mean_forcing(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

the mean parameter for forcing
