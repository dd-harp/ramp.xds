# Get the trend parameters

Get the trend parameters for an **MY** trivial model

## Usage

``` r
# S3 method for class 'MY'
get_trend(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

a list, the value of `trend_par`
