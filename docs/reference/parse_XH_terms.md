# Pull XH Terms

Pull the stored values of the EIR and the FoI and compute NI

## Usage

``` r
parse_XH_terms(xds_obj, i = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- i:

  host species index

## Value

an **`xds`** model object
