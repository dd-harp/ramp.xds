# Compute as DDE

If `class(xds_obj$xde) == "dde"` don't change anything

## Usage

``` r
# S3 method for class 'dde'
ode_to_dde(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

a **`ramp.xds`** model object
