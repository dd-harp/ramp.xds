# Make indices for all the model variables

Make indices for all the model variables

## Usage

``` r
make_indices(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** model object
