# Initialize VC

To compute vectorial capacity, we need to

## Usage

``` r
# S3 method for class 'GeM'
setup_V_ix(xds_obj, i)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

- i:

  the vector species index

## Value

a **`ramp.xds`** model object
