# Compute transmission, the dynamic case

Compute transmission terms with a dynamic mixing matrix

## Usage

``` r
transmission_dynamics(t, y, xds_obj)
```

## Arguments

- t:

  current simulation time

- y:

  state vector

- xds_obj:

  an **`xds`** model object

## Value

an `xds` object
