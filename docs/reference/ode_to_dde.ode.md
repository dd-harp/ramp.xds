# Compute as DDE

If `class(xds_obj$xde) == "ode"` change it to `dde`

## Usage

``` r
# S3 method for class 'ode'
ode_to_dde(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

a **`ramp.xds`** model object
