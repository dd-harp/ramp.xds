# Make TaR

Make a time at risk matrix (TaR) from a time spent matrix and a
circadian function

## Usage

``` r
make_TaR(pars, t = 0)
```

## Arguments

- pars:

  an `xds` object

- t:

  the time

## Value

an `xds` object
