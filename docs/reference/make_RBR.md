# Compute and attach the relative biting rates

Compute the availability for the pathogen's hosts for blood feeding

## Usage

``` r
make_RBR(t, pars, y)
```

## Arguments

- t:

  the time

- pars:

  an `xds` object

- y:

  state vector

## Value

an `xds` object
