# Get the shock parameters

Get the parameters for the perturbation function forcing the model

## Usage

``` r
get_shock(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

shock parameters

## Note

Dispatches on `forced_by`
