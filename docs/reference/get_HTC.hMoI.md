# Compute the get_HTC for the hMoI model

Implements `get_HTC` for the hMoI model with demography.

## Usage

``` r
# S3 method for class 'hMoI'
get_HTC(xds_obj, i)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- i:

  the host species index

## Value

a [numeric](https://rdrr.io/r/base/numeric.html) vector
