# Setup an EIP Bionomic Object

Set up an object to compute the EIP

## Usage

``` r
setup_eip_obj(eip, MY_obj)
```

## Arguments

- eip:

  the mosquito patch emigration rate

- MY_obj:

  an **`MY`** model object

## Value

a **`MY`** model object
