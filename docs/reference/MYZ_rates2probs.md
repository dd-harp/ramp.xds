# Compute probabilities from rates

This method dispatches on `MYZname`.

## Usage

``` r
MYZ_rates2probs(MYZpar, runtime)
```

## Arguments

- MYZpar:

  the `MYZ` model object

- runtime:

  the model `runtime` parameters

## Value

a [list](https://rdrr.io/r/base/list.html) with baseline values
