# readRDS for `xds` Objects

Read the `xds` object using `readRDS` and rebuild the forcing functions

## Usage

``` r
readXDS(filename)
```

## Arguments

- filename:

  the file name

## Value

an **`xds`** model object
