# Compute beta

This function computes the mixing matrix, beta

## Usage

``` r
compute_beta(t, y, xds_obj)
```

## Arguments

- t:

  current simulation time

- y:

  state vector

- xds_obj:

  an **`xds`** model object

## Value

an `xds` object
