# Set `xds` for `dts`

Do not change `xds`

## Usage

``` r
# S3 method for dts
xds_dde(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** modelobject

## Value

a **`ramp.xds`** model object
