# Get phase

Get the parameter that sets the phase for a seasonally forced model

## Usage

``` r
get_season_bottom(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

the phase parameter(s)
