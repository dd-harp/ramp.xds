# Set `xds` to `dde`

Creates and returns structured template for an

## Usage

``` r
xds_dde(xds_obj)
```

## Arguments

- xds_obj:

  a **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
