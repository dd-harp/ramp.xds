# Change initial values for `RMdts` (**MY**)

Change initial values for `RMdts` (**MY**)

## Usage

``` r
# S3 method for class 'RMdts'
change_MY_inits(xds_obj, s, options)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

- options:

  a named list

## Value

a [list](https://rdrr.io/r/base/list.html)
