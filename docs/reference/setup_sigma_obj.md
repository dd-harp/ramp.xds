# Setup a Patch Emigration Bionomic Object

Set up an object to compute the human fraction, \\sigma\\

## Usage

``` r
setup_sigma_obj(sigma, MY_obj)
```

## Arguments

- sigma:

  the mosquito patch emigration rate

- MY_obj:

  an **`MY`** model object

## Value

a **`MY`** model object
