# Check the XY Interface

Run a set of consistency checks for the `XY_interface`

## Usage

``` r
check_XY_interface(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** model object
