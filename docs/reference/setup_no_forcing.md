# none set up for exogenous forcing

This sets up the `none` option for exogenous forcing: no forcing.

## Usage

``` r
setup_no_forcing(pars)
```

## Arguments

- pars:

  an **`xds`** object

## Value

an **`xds`** object
