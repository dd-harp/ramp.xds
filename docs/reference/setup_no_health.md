# none set up for exogenous health

This sets up the `none` option for exogenous health: no health.

## Usage

``` r
setup_no_health(xds_obj)
```

## Arguments

- xds_obj:

  an **`ramp.xds`** model object

## Value

a **`ramp.xds`** model object
