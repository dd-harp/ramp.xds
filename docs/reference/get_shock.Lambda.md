# Get the shock parameters

Get the shock parameters for a model forced by emergence

## Usage

``` r
# S3 method for class 'Lambda'
get_shock(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

a list, the value of `shock_par`
