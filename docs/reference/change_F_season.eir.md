# Change F_season

Change the seasonality function when `forced_by = "eir"`

## Usage

``` r
# S3 method for class 'eir'
change_F_season(F, xds_obj, ix = 1)
```

## Arguments

- F:

  a seasonal pattern function

- xds_obj:

  an **`xds`** model object

- ix:

  the species index

## Value

an **`xds`** object
