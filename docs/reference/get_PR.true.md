# Get the *Pf*PR from a Malaria Model

Return the true *Pf*PR

## Usage

``` r
# S3 method for class 'true'
get_PR(xds_obj, i = 1, method = "true")
```

## Arguments

- xds_obj:

  an **`xds`** object

- i:

  the host species index

- method:

  the method used for computing *Pf*PR

## Value

the true PR
