# Setup a Human Fraction Bionomic Object

Set up an object to compute the human fraction, \\phi\\

## Usage

``` r
setup_phi_obj(phi, L_obj)
```

## Arguments

- phi:

  the human fraction

- L_obj:

  an **`MY`** model object

## Value

a **`MY`** model object
