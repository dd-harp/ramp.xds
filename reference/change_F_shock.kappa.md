# Change F_shock

Change the shock function when `forced_by = "kappa"`

## Usage

``` r
# S3 method for class 'kappa'
change_F_shock(F, xds_obj, ix = 1)
```

## Arguments

- F:

  new shock function

- xds_obj:

  an **`xds`** model object

- ix:

  the species index

## Value

an **`xds`** object
