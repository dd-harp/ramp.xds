# Check the XY Interface

Run a set of consistency checks for the `blood_feeding`

## Usage

``` r
check_blood_feeding(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** object
