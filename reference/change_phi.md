# Change phi

Change the density independent mortality rate for basicL for a static
model

## Usage

``` r
change_phi(phi, xds_obj, s)
```

## Arguments

- phi:

  the density independent mortality rate

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

an **`xds`** model object
