# Setup a Human Fraction Bionomic Object

Set up an object to compute the human fraction, \\theta\\

## Usage

``` r
setup_theta_obj(theta, L_obj)
```

## Arguments

- theta:

  the human fraction

- L_obj:

  an **`MY`** model object

## Value

a **`MY`** model object
