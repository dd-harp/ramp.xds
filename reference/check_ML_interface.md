# Check the ML Interface

Run a set of consistency checks for the `ML_interface`

## Usage

``` r
check_ML_interface(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object

## Value

an **`xds`** model object
