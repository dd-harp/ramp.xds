# The skill set (**L** module)

The skill set for an **L** module is a list that summarizes capabilities
and compatibilities

## Usage

``` r
skill_set_L(Lname)
```

## Arguments

- Lname:

  the **L** module name

## Value

*L* module skill set, as a list
