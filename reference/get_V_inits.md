# A function to set up V_obj

Get and display the stored set of initial values.

## Usage

``` r
get_V_inits(xds_obj, i = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- i:

  the host species index

## Value

the initial values, as a named list
