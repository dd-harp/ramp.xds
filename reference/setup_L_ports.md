# Set up L ports

Adds port objects and default values for the habitat search weights.

## Usage

``` r
setup_L_ports(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

an **`xds`** object
