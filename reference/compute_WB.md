# Compute available blood hosts of the i^th species

Compute the available for the pathogen's hosts for blood feeding

## Usage

``` r
compute_WB(t, y, xds_obj)
```

## Arguments

- t:

  the time

- y:

  state vector

- xds_obj:

  an **`xds`** model object

## Value

an `xds` object
