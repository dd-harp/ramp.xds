# Make TaR

Make a time at risk matrix (TaR) from a time spent matrix and a
circadian function

## Usage

``` r
compute_TaR(xds_obj, t = 0)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- t:

  the time

## Value

an `xds` object
