# Get seasonality parameter `pw`

Get the `pw` parameter for the seasonality function

## Usage

``` r
get_season_pw(xds_obj, s = 1)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

## Value

the pw parameter(s)
