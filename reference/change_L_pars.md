# Set **L** Component Parameters

Set **L** Component Parameters

## Usage

``` r
change_L_pars(xds_obj, s = 1, options = list())
```

## Arguments

- xds_obj:

  an **`xds`** model object

- s:

  the vector species index

- options:

  a named list

## Value

an `xds` object
