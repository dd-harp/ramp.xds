# Get the last state

Get the last state

## Usage

``` r
get_last(xds_obj, parse = FALSE)
```

## Arguments

- xds_obj:

  an **`xds`** model object

- parse:

  if TRUE return a named list

## Value

a [numeric](https://rdrr.io/r/base/numeric.html) vector
