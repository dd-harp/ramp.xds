# Get mean forcing

Get the mean density of infectious mosquitoes, for a trivial **MY**
model

## Usage

``` r
# S3 method for class 'fqZ'
get_mean_forcing(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object
