# The trivial function

A function that returns 0

## Usage

``` r
One_tV(t, V = list())
```

## Arguments

- t:

  an arbitrary input

- V:

  an arbitrary input

## Value

a vector of ones of length x
