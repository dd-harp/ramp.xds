# Get mean forcing

Get `Z`, the mean density of infectious mosquitoes, for an **MY**
trivial model

## Usage

``` r
# S3 method for class 'MY'
get_mean_forcing(xds_obj)
```

## Arguments

- xds_obj:

  an **`xds`** model object
