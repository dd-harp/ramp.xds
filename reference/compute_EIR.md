# Compute EIR

This function computes the EIR for each stratum of each host species

## Usage

``` r
compute_EIR(t, y, xds_obj)
```

## Arguments

- t:

  current simulation time

- y:

  state vector

- xds_obj:

  an **`xds`** model object

## Value

an `xds` object
